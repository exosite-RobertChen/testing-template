protocol:
  name: onep
provisioning:
  auth_type: token

tags_schema:
    description: |
      Tags validation Schema.
      IMPORTANT: properties types MUST be 'string'.
    format: jsonSchema
    type: object
    properties:
      app:
        enum: [ object ]